"use client";

/** Minimal stub — real upload in later weeks */
export function UploadDropzone() {
  return (
    <div className="rounded-lg border-2 border-dashed border-neutral-300 p-10 text-center dark:border-neutral-600">
      <p className="text-sm text-neutral-500">UploadDropzone（スタブ）</p>
      <p className="mt-1 text-xs text-neutral-400">
        写真をドロップ / 選択（未実装）
      </p>
    </div>
  );
}
